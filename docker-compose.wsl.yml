services:
  mysql:
    ports:
      - "${MYSQL_HOST_PORT:-13306}:3306"

  selenium-hub:
    ports:
      - "${SELENIUM_HOST_PORT:-14444}:4444"

  scraper:
    ports:
      - "${SCRAPER_HOST_PORT:-18082}:8081"
